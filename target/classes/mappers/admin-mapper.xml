<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
<mapper namespace="Admin">
	<select id="login" resultType="int" parameterType="com.kedu.admin.company.CompanyDTO">
		select count(*) from company where admin_email = #{admin_email} and pw = #{pw}
	</select>
	
	<select id="emailAuth" resultType="int" parameterType="com.kedu.admin.company.CompanyDTO">
		select count(*) from company where admin_email = #{admin_email}
	</select>
	
	<update id="updatePw" 	>
		update company set pw = #{pw} where admin_email = #{admin_email}
	</update>
</mapper>