<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MemberMapper">
	<!-- 사원 목록 조회 -->
	<select id="getAllMember"
		resultType="com.kedu.members.member.MemberDTO">
		SELECT * from member ORDER BY signup_at DESC
	</select>

	<!-- 초대코드용 -->
	<select id="getMemberByEmailAndCompany" parameterType="map"
		resultType="com.kedu.members.member.MemberDTO">
		SELECT *
		FROM member
		WHERE email = #{email}
		AND company_code =
		#{company_code}
	</select>

	<!-- 업데이트용 -->
	<update id="updateMemberStatus"
		parameterType="com.kedu.members.member.MemberDTO">
		UPDATE member
		SET status = #{status}
		WHERE email = #{email}
		AND company_code = #{company_code}
	</update>


</mapper>
